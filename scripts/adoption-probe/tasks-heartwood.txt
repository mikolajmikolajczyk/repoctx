# Adoption-probe tasks for the heartwood codebase (Rust, radicle node) — the
# second probe corpus, a different language ecosystem from madside (TS).
# Mirrors tasks.txt structure; symbols verified to exist via repoctx.
# One per line; blank lines + # ignored.
Where is the function verify_signature defined? Answer with the file and line.
What calls the function refs_announcement?
Give a high-level overview of this repository's architecture: its main subsystems and entry points.
List everywhere canonical_head is used across the codebase.
What does the file that defines the Storage trait import?

# --- harder / exploratory (stronger grep reflex) — for the full sweep ---
Find all call sites and string occurrences of the term "gossip" in this repo.
Which files import the storage module, and does the CLI layer import it directly?
Trace what happens when a repository is fetched: what functions are involved?
Is there any dead code (functions defined but never called)?
What are the most central / most-called functions in this codebase?
