# Gain analytics

> Placeholder. Full page (philosophy, baseline rules, privacy switches) lands with Radicle issue `c328bfc`.

Until then: `repoctx gain --help` lists the flags. Defaults: last 30 days, no query text stored. Disable with `--no-record` or `RUST_REPOCTX_NO_RECORD=1`.
