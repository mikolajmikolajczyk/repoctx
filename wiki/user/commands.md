# Commands reference

> Placeholder. M0 reference (`index`, `symbols`, `status`, `gain`) lands with Radicle issue `b19abbe`; M1 (`outline`, `definition`, `context`) with `38865bb`.

For now, every command supports `--help`:

```sh
repoctx --help
repoctx <command> --help
```
